# 瞬 V1.0.1 开发文档

**技术实现文档**

---

## 📋 文档信息

- **版本**: V1.0.1
- **日期**: 2026年2月23日
- **作者**: 开发团队
- **状态**: 正式版

---

## 🏗️ 技术架构

### 整体架构
```
┌─────────────────────────────────────┐
│         Presentation Layer          │
│  (Screens, Widgets, UI Components)  │
├─────────────────────────────────────┤
│         Business Logic Layer        │
│    (Providers, State Management)    │
├─────────────────────────────────────┤
│          Data Layer                 │
│  (Models, Services, Storage)        │
├─────────────────────────────────────┤
│         Platform Layer              │
│    (Flutter Framework, Plugins)     │
└─────────────────────────────────────┘
```

### 技术选型
- **UI框架**: Flutter 3.27.1
- **编程语言**: Dart 3.x
- **状态管理**: Provider
- **路由管理**: GoRouter
- **本地存储**: SharedPreferences
- **权限管理**: permission_handler

---

## 📦 核心模块

### 1. 启动模块 (Splash)
**文件**: `screens/splash_screen.dart`

**功能**:
- 显示品牌Logo"瞬"
- 光球呼吸动画
- 2.5秒后自动跳转

**关键代码**:
```dart
AnimationController _controller = AnimationController(
  duration: const Duration(milliseconds: 2500),
  vsync: this,
);

_scaleAnimation = Tween<double>(begin: 0.8, end: 1.05).animate(
  CurvedAnimation(parent: _controller, curve: Curves.easeInOut),
);
```

---

### 2. 登录模块 (Login)
**文件**: `screens/login_screen.dart`

**功能**:
- 手机号输入验证
- 验证码发送（60秒倒计时）
- 登录验证（Mock: 123456）

**状态管理**:
```dart
class AuthProvider extends ChangeNotifier {
  String? _phone;
  bool _isLoggedIn = false;
  
  Future<bool> login(String phone, String code) async {
    if (code == '123456') {
      _phone = phone;
      _isLoggedIn = true;
      notifyListeners();
      return true;
    }
    return false;
  }
}
```

---

### 3. 匹配模块 (Match)
**文件**: `widgets/match_tab.dart`, `providers/match_provider.dart`

**功能**:
- 每日20次匹配机会
- 光球呼吸动画
- 2.5秒匹配延迟
- 取消匹配功能
- 招呼语系统（二选一）

**核心逻辑**:
```dart
Future<void> startMatch() async {
  if (_matchCount <= 0 || _isMatching) return;
  
  _isMatching = true;
  _matchedUser = null;
  notifyListeners();
  
  await Future.delayed(const Duration(milliseconds: 2500));
  
  if (!_isMatching) return; // 检查是否被取消
  
  _matchedUser = _generateMockUser();
  _isMatching = false;
  
  // 匹配成功，立即消耗次数
  consumeMatch();
  notifyListeners();
}

void cancelMatch() {
  if (_isMatching) {
    _isMatching = false;
    _matchedUser = null;
    notifyListeners();
  }
}
```

**消耗逻辑**:
- ✅ 匹配成功弹出卡片 → 消耗1次
- ❌ 匹配中取消 → 不消耗
- ❌ 卡片上点"算了" → 不消耗（已消耗过）

---

### 4. 聊天模块 (Chat)
**文件**: `screens/chat_screen.dart`, `providers/chat_provider.dart`

**功能**:
- 消息发送/接收
- 长按复制/撤回
- 渐进式解锁（15分钟/30分钟）
- 语音通话入口

**解锁机制**:
```dart
Timer.periodic(const Duration(seconds: 1), (_) {
  final elapsed = DateTime.now().difference(thread.createdAt);
  _elapsedMinutes = elapsed.inMinutes;
  
  if (_elapsedMinutes >= 15 && thread.unlockLevel < 1) {
    updateUnlockLevel(threadId, 1); // 解锁头像
  }
  if (_elapsedMinutes >= 30 && thread.unlockLevel < 2) {
    updateUnlockLevel(threadId, 2); // 解锁主页
  }
});
```

**消息操作**:
```dart
// 长按消息显示操作菜单
GestureDetector(
  onLongPress: () async {
    final action = await AppDialog.showMessageActions(
      context,
      isMe: message.isMe,
      canRecall: canRecall, // 2分钟内可撤回
    );
    
    if (action == 'copy') {
      await Clipboard.setData(ClipboardData(text: message.content));
    } else if (action == 'recall') {
      chatProvider.recallMessage(threadId, message.id);
    }
  },
  child: MessageBubble(...),
)
```

---

### 5. 好友模块 (Friend)
**文件**: `widgets/friends_tab.dart`, `providers/friend_provider.dart`

**功能**:
- 发送好友请求（30分钟后）
- 接受/拒绝请求
- 好友列表
- 设置备注
- 好友聊天（无时间限制）

**请求机制**:
```dart
class FriendProvider extends ChangeNotifier {
  final Map<String, Friend> _friends = {};
  final Map<String, FriendRequest> _requests = {};
  
  void sendFriendRequest(User user, String? message) {
    final request = FriendRequest(
      id: const Uuid().v4(),
      fromUser: user,
      message: message,
      createdAt: DateTime.now(),
    );
    _requests[request.id] = request;
    notifyListeners();
  }
  
  void acceptFriendRequest(String requestId) {
    final request = _requests[requestId];
    if (request != null) {
      _friends[request.fromUser.id] = Friend(
        id: request.fromUser.id,
        user: request.fromUser,
        becameFriendAt: DateTime.now(),
      );
      notifyListeners();
    }
  }
}
```

---

### 6. 权限模块 (Permission)
**文件**: `utils/permission_manager.dart`

**功能**:
- 通知权限
- 相机权限
- 相册权限
- 麦克风权限
- 位置权限（每次确认）

**权限请求流程**:
```dart
static Future<bool> requestCameraPermission(BuildContext context) async {
  final status = await Permission.camera.status;
  
  if (status.isGranted) {
    return true;
  }
  
  if (status.isDenied) {
    final shouldRequest = await _showPermissionDialog(
      context,
      title: '开启相机权限',
      content: '开启相机权限后，你可以拍摄照片设置头像',
      icon: Icons.camera_alt_outlined,
    );
    
    if (shouldRequest == true) {
      final result = await Permission.camera.request();
      return result.isGranted;
    }
  }
  
  if (status.isPermanentlyDenied) {
    await _showGoToSettingsDialog(context, ...);
  }
  
  return false;
}
```

---

### 7. 提示模块 (Toast)
**文件**: `widgets/app_toast.dart`

**功能**:
- 底部浮动提示
- 底部弹窗确认
- 消息操作菜单

**设计特点**:
```dart
class AppToast {
  static void show(BuildContext context, String message, {bool isError = false}) {
    ScaffoldMessenger.of(context).showSnackBar(
      SnackBar(
        content: Text(message, textAlign: TextAlign.center),
        backgroundColor: isError 
            ? AppColors.error.withOpacity(0.15)
            : AppColors.white12,
        behavior: SnackBarBehavior.floating,
        shape: RoundedRectangleBorder(
          borderRadius: BorderRadius.circular(12),
        ),
        margin: const EdgeInsets.only(bottom: 80, left: 40, right: 40),
        elevation: 0,
      ),
    );
  }
}
```

---

## 🎨 UI/UX设计

### 主题配置
**文件**: `config/theme.dart`

```dart
class AppColors {
  static const pureBlack = Color(0xFF000000);
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFB8B8B8);
  static const textTertiary = Color(0xFF808080);
  static const brandBlue = Color(0xFF4A90E2);
  static const deepSeaBlue = Color(0xFF0A0E27);
  static const cardBg = Color(0xFF1A1A1A);
  static const error = Color(0xFFFF4444);
}
```

### 字体系统
```dart
TextTheme(
  displayLarge: TextStyle(
    fontSize: 32,
    fontWeight: FontWeight.w300,
    letterSpacing: 8,
  ),
  bodyLarge: TextStyle(
    fontSize: 16,
    fontWeight: FontWeight.w300,
    letterSpacing: 0.5,
  ),
)
```

---

## 💾 数据存储

### 本地存储
**文件**: `services/storage_service.dart`

```dart
class StorageService {
  static late SharedPreferences _prefs;
  
  static Future<void> init() async {
    _prefs = await SharedPreferences.getInstance();
  }
  
  // 匹配次数
  static int getMatchCount() {
    return _prefs.getInt('match_count') ?? 20;
  }
  
  static Future<void> saveMatchCount(int count) async {
    await _prefs.setInt('match_count', count);
  }
  
  // 每日重置时间
  static DateTime? getLastResetDate() {
    final timestamp = _prefs.getInt('last_reset_date');
    return timestamp != null ? DateTime.fromMillisecondsSinceEpoch(timestamp) : null;
  }
}
```

---

## 🔄 状态管理

### Provider架构
```dart
MultiProvider(
  providers: [
    ChangeNotifierProvider(create: (_) => AuthProvider()),
    ChangeNotifierProvider(create: (_) => MatchProvider()),
    ChangeNotifierProvider(create: (_) => ChatProvider()),
    ChangeNotifierProvider(create: (_) => FriendProvider()),
  ],
  child: MaterialApp.router(...),
)
```

---

## 🚀 性能优化

### 1. 内存优化
- 所有Controller正确释放
- 使用const构造函数
- ListView.builder懒加载

### 2. 渲染优化
- Consumer精确监听
- 减少不必要的重建
- 使用RepaintBoundary

### 3. 启动优化
- 延迟初始化
- 异步加载
- 缓存优化

---

## 📱 平台适配

### Android配置
**文件**: `android/app/build.gradle`

```gradle
android {
    compileSdkVersion 34
    
    defaultConfig {
        applicationId "com.sunliao.app"
        minSdkVersion 21
        targetSdkVersion 34
        versionCode 2
        versionName "1.0.1"
    }
}
```

### 权限配置
**文件**: `android/app/src/main/AndroidManifest.xml`

```xml
<uses-permission android:name="android.permission.INTERNET"/>
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
<uses-permission android:name="android.permission.CAMERA"/>
<uses-permission android:name="android.permission.RECORD_AUDIO"/>
<uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
```

---

## 🧪 测试

### 单元测试
- Provider逻辑测试
- 工具类测试
- 数据模型测试

### 集成测试
- 完整流程测试
- 页面跳转测试
- 状态同步测试

### UI测试
- 组件渲染测试
- 交互测试
- 动画测试

---

## 📊 代码统计

- **总代码行数**: 5000+
- **Dart文件**: 20个
- **平均文件大小**: 250行
- **注释覆盖率**: 15%

---

## 🔧 开发工具

- **IDE**: Android Studio / VS Code
- **Flutter SDK**: 3.27.1
- **Dart SDK**: 3.x
- **版本控制**: Git

---

**文档版本**: V1.0  
**最后更新**: 2026年2月23日


