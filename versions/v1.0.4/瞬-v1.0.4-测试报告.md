# 瞬 v1.0.4 测试报告

## 测试目标

恢复 Flutter 自动化测试基线，并验证 `v1.0.4` 聊天主链路优化没有引入回归。

## 本轮检查项

- 权限插件 mock 是否生效
- smoke test 是否具备完整路由上下文
- 设置与资料相关测试是否与现代码一致
- 聊天消息撤回是否受 2 分钟业务规则约束
- WebSocket 重复消息是否会重复累计未读
- 对端已读回执是否按 `lastReadMessageId` 精确生效
- 远端历史消息回补时是否会丢失本地图片预览
- 远端历史延迟时，本地近期已发送消息是否会被错误清掉
- 失败图片消息是否会被错误地按文本重发
- 本地失败/发送中消息是否会与远端成功消息形成重复记录
- 自己发送的消息被 `msg.new` 回推时是否会错误增加未读或形成重复
- 自己发送的消息如果通过 `msg.new` 自回环成功，是否会补齐亲密度积分
- 连续图片消息在远端回补时，是否会因为同为 `[图片]` 而误归并到错误本地消息
- 会话标记已读后，通知中心同线程聊天通知是否会同步标记已读
- 同一条聊天消息重复写入通知中心时，是否会被正确去重

## 当前状态

- 文档已创建
- 代码修复已完成第一批与聊天链路第二批
- 最新一轮 `flutter test` 已通过

## 最新结果

- 执行日期：`2026-03-12`
- 执行命令：`flutter test --reporter expanded`
- 结果：`All tests passed!`

## 本轮修复验证点

- 权限插件测试 mock 生效
- `SettingsProvider` 过期测试已清理
- `main_screen_smoke_test` 已具备完整路由与 Provider 上下文
- 透明主页与竖版全屏背景状态已恢复一致
- `ChatProvider` 拆分后相关 provider 测试通过
- 消息撤回超过 2 分钟会被拒绝
- 重复 `msg.new` 事件不会再次增加未读
- 对端已读仅更新到回执指定消息
- 远端图片消息未返回可用地址时，本地预览仍可保留
- 远端历史延迟时，近期已发送消息会在保护窗口内保留
- 失败图片消息不会再走文本重发路径
- 本地失败/发送中消息在远端成功回补后会正确归并
- 自己发送的消息被 `msg.new` 回推时会归并到本地消息，不再产生未读和重复
- 自己发送的消息如果通过 `msg.new` 自回环成功，会与 `ACK` 链路一样补齐亲密度积分
- 连续图片消息在远端回补时会按阅后即焚和质量指纹归并，避免错绑本地预览
- 会话被标记已读时，同线程聊天通知也会同步标记为已读
- 同一条聊天消息重复进入通知中心时会按消息维度幂等更新，不再生成重复通知项

## 本轮执行记录

- 目标文件：`test/providers/chat_provider_test.dart`
- 定向回归结果：`All tests passed!`
- 全量回归结果：`All tests passed!`
- 新增图片误归并回归：`All tests passed!`
- 新增通知中心已读联动回归：`All tests passed!`
- 新增聊天通知幂等回归：`All tests passed!`

## 下一步

1. 继续推进聊天离线补偿与会话恢复一致性。
2. 为图片消息和通知联动补齐回归用例。
3. 在 `v1.0.4` 内继续做聊天核心链路迭代。
